apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "jinja-template-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "jinja-template-operator.labels" . | nindent 4 }}
    control-plane: controller-manager
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "jinja-template-operator.selectorLabels" . | nindent 6 }}
      control-plane: controller-manager
  template:
    metadata:
      {{- if or .Values.podAnnotations .Values.operator.rawObjects.allowlist }}
      annotations:
        {{- if .Values.operator.rawObjects.allowlist }}
        checksum/raw-object-allowlist: {{ toYaml .Values.operator.rawObjects.allowlist | sha256sum }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      labels:
        {{- include "jinja-template-operator.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        control-plane: controller-manager
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "jinja-template-operator.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: 10
      containers:
        - name: manager
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - ./manager
          args:
            - --metrics-bind-address=:{{ .Values.metrics.port }}
            - --health-probe-bind-address=:{{ .Values.healthProbe.port }}
            {{- if .Values.leaderElection.enabled }}
            - --leader-elect=true
            {{- end }}
            - --default-owner-reference={{ .Values.operator.defaultOwnerReference }}
            {{- if .Values.operator.rawObjects.allowlist }}
            - --raw-object-allowlist-file=/etc/jinja-template-operator/raw-objects/allowlist.yaml
            {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.healthProbe.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.operator.rawObjects.allowlist }}
          volumeMounts:
            - name: raw-object-allowlist
              mountPath: /etc/jinja-template-operator/raw-objects
              readOnly: true
          {{- end }}
      {{- if .Values.operator.rawObjects.allowlist }}
      volumes:
        - name: raw-object-allowlist
          configMap:
            name: {{ include "jinja-template-operator.fullname" . }}-raw-object-allowlist
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
